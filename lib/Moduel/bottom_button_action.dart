import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';

class bottom_button_action {
  Icon noactionhome = Icon(Icons.home_outlined);
  Icon noactionsearch = Icon(Icons.search);
  Icon noactionnotifications = Icon(Icons.notifications_none);
  Icon noactionmail = Icon(Icons.mail_outline);
  Icon actionhome = Icon(Icons.home, size: 30);
  Icon actionsearch = Icon(Icons.search, size: 30);
  Icon actionnotifications = Icon(
    Icons.notifications,
    color: Colors.black,
    size: 30,
  );
  Icon actionmail = Icon(
    Icons.mail,
    size: 30,
  );
  Icon home = Icon(Icons.home, size: 30);
  Icon search = Icon(Icons.search);
  Icon notifications = Icon(Icons.notifications_none);
  Icon mail = Icon(Icons.mail_outline);
}
